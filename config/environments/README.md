# Environment Configuration

This folder stores safe environment templates and documentation.

Do not store active credentials here.

Use placeholders such as:

OPENAI_API_KEY=replace-with-local-value
DATABASE_PASSWORD=replace-with-local-value
EMAIL_PASSWORD=replace-with-local-value

Actual values belong in the local .env file, which must not be committed.
